This repository is currently used to store the front-end and back-end code for BachelorArbeit testcode.

The front-end uses React as the basic framework and TypeScript as the development language, while the back-end uses Python as the development language and PostgreSQL as the database.
